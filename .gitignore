.env
.env.*
secrets.json
*.pem
*.key

.venv/
env/
venv/
ENV/
Hyperion/

**/__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.installed.cfg
*.egg
MANIFEST

.pytest_cache/
.coverage
htmlcov/

db.sqlite3
*.log
*.sqlite3
datadump.json
backups/

media/
static/
# STATIC_ROOT is BASE_DIR/static (collectstatic output), so `static/` has to
# stay ignored - but the pattern matches at any depth, and an app's own
# static/ directory is source, not build output.
!*/static/
staticfiles/

.idea/
*.iml
*.iws
.vscode/

.claude/

.DS_Store
.DS_Store?
ehthumbs.db
Icon?
Thumbs.db

# Ad-hoc exports from `manage.py export_point_history` etc. Root-level only,
# so committed CSV test fixtures under sports/tests/fixtures/ are unaffected.
/*.csv
